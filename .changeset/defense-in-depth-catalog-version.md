---
"agentic": minor
---

Version Defense in Depth in DEFENSE_IN_DEPTH.md

This repo's `DEFENSE_IN_DEPTH.md` is the catalog and carries `Version:`. Target repos use the same
file and the same `Version:` line, so you can see what each repo is on by comparing the two files.
